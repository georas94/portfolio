{% extends 'base.html.twig' %}

{% block body %}
    <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
        {% for a in ao %}
            {% include 'ao/_card.html.twig' %}
        {% endfor %}
    </div>
{% endblock %}